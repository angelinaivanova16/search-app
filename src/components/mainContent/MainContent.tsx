import s from './mainContent.module.scss';

export const MainContent = () => {
  return (
    <main>
      <h2 className={s.title}>Enter what you're searching for, then press Enter.</h2>
    </main>
  )
}